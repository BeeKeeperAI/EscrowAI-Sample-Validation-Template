# Fix numpy version first to avoid binary incompatibility
numpy==1.24.3

# Use CPU-only torch to reduce download size
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.1+cpu
torchvision==0.15.2+cpu

# Core dependencies with compatible versions
Pillow>=9.0.0
EnclaveSDK
scikit-learn==1.3.0
pandas==2.0.3

# Fastai core dependencies (minimal, no NLP)
fastcore==1.5.29
fastdownload==0.0.7
fastprogress==1.0.3

# Fastai without heavy dependencies
fastai==2.7.12